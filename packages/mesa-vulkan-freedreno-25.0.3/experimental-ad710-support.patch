diff -uNr mesa-vulkan-freedreno-25.0.3.orig/src/freedreno/common/freedreno_devices.py mesa-vulkan-freedreno-25.0.3-a710/src/freedreno/common/freedreno_devices.py
--- mesa-vulkan-freedreno-25.0.3.orig/src/freedreno/common/freedreno_devices.py	2025-04-02 13:35:11.000000000 -0300
+++ mesa-vulkan-freedreno-25.0.3-a710/src/freedreno/common/freedreno_devices.py	2025-07-04 20:35:00.000000000 +0000
@@ -10,6 +10,24 @@
 # SPDX-License-Identifier: MIT
 import collections

+##
+# Adreno A710 magic registers (PLACEHOLDER)
+# Replace these with real ones extracted from libadreno.so
+##
+a710_magic_regs = [
+    (0x305080, 0x0),
+    (0x305084, 0x0),
+    (0x305100, 0x3f),
+    (0x305140, 0x12345678),
+]
+
+a710_raw_magic_regs = [
+    (0x305020, 0xdeadbeef),
+    (0x305030, 0xabadcafe),
+]
+
 from .freedreno_dev_info import GPUId, CHIP, A6xxGPUInfo
 from .a6xx import a6xx_base, a6xx_gen1, a730_magic_regs, a730_raw_magic_regs, a7xx_base, a7xx_gen1

 add_gpus([
+    GPUId(chip_id=0x07010000, name="FD710"),
+    GPUId(chip_id=0xffff07010000, name="FD710"),
+], A6xxGPUInfo(
+    CHIP.A7XX,
+    [a7xx_base, a7xx_gen1],
+    num_ccu = 4,
+    tile_align_w = 64,
+    tile_align_h = 32,
+    num_vsc_pipes = 32,
+    cs_shared_mem_size = 32 * 1024,
+    wave_granularity = 2,
+    fibers_per_sp = 128 * 2 * 16,
+    highest_bank_bit = 16,
+    magic_regs = a710_magic_regs,
+    raw_magic_regs = a710_raw_magic_regs,
+))
+
 add_gpus([
     GPUId(chip_id=0x07030001, name="FD730"),
     GPUId(chip_id=0xffff07030001, name="FD730"),
diff -uNr mesa-vulkan-freedreno-25.0.3.orig/src/freedreno/drm-shim/freedreno_noop.c mesa-vulkan-freedreno-25.0.3-a710/src/freedreno/drm-shim/freedreno_noop.c
--- mesa-vulkan-freedreno-25.0.3.orig/src/freedreno/drm-shim/freedreno_noop.c	2025-04-02 13:35:11.000000000 -0300
+++ mesa-vulkan-freedreno-25.0.3-a710/src/freedreno/drm-shim/freedreno_noop.c	2025-07-04 20:35:00.000000000 +0000
@@ -235,6 +235,11 @@
       .gmem_size = 1024 * 1024 + 512 * 1024,
    },
    {
+      .gpu_id = 710,
+      .chip_id = 0x07010000,
+      .gmem_size = 2 * 1024 * 1024,
+   },
+   {
       .gpu_id = 730,
       .chip_id = 0x07030001,
       .gmem_size = 2 * 1024 * 1024,
